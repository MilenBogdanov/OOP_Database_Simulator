package project.OOP2.f22621615.enums;

/**
 * The {@code DataType} enum represents different data types that can be used in the application.
 * Each data type corresponds to a specific type of data that can be stored in the database.
 */
public enum DataType {
    INTEGER,
    FLOAT,
    STRING,
    NULL;
}