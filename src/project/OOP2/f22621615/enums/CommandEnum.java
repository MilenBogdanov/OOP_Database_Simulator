package project.OOP2.f22621615.enums;
/**
 * The {@code CommandEnum} enum represents different commands that can be used in the application.
 * Each command corresponds to a specific action that can be performed.
 */
public enum CommandEnum {
    OPEN,
    LOAD,
    CLOSE,
    SAVE,
    SAVEAS,
    HELP,
    EXIT,
    SHOWTABLES,
    DESCRIBE,
    PRINT,
    EXPORT,
    SELECT,
    ADDCOLUMN,
    UPDATE,
    DELETE,
    INSERT,
    RENAME,
    COUNT,
    AGGREGATE
}